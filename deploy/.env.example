# Copier ce fichier en deploy/.env et renseigner les valeurs réelles
# Ne jamais committer deploy/.env dans git

# CORS — origine(s) autorisée(s) pour le backend
# Remplacer par le domaine réel du frontend en production
CORS_ALLOWED_ORIGINS=https://votre-domaine.com
